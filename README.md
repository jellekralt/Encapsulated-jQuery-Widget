Encapsulated-Widget
===================
This encapsulated widget provides basic jQuery functionality through Zepto. The widget file can be added to a page without causing conflicts with the jQuery version that is used on the page.
